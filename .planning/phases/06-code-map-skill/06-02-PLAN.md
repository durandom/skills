---
phase: 06-code-map-skill
plan: 02
type: execute
---

<objective>
Create templates for each documentation level.

Purpose: Provide consistent, copy-and-fill structures for map documents.
Output: Templates for MAP.md, L0-architecture.md, and L1-domain.md
</objective>

<execution_context>
@.claude/skills/create-plans/workflows/execute-phase.md
@.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-code-map-skill/BRIEF.md
@.planning/phases/06-code-map-skill/06-01-SUMMARY.md
@.claude/skills/code-map/references/format-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MAP.md template</name>
  <files>.claude/skills/code-map/templates/MAP.md</files>
  <action>
Create entry point template with:

1. **Quick Start for Agents** section:
   - Numbered steps for bootstrap
   - How to navigate the map
   - Key commands (grep patterns)

2. **Format Specification** section (brief):
   - Zoom levels explained
   - Anchor/reference syntax
   - Link to full format-spec.md

3. **Domains Index** table:
   - | Domain | Path | Purpose |
   - Placeholder rows with `[domain-name]`

4. **Code Statistics** section:
   - Total files, languages, last update
   - Use `[placeholder]` syntax

5. **Navigation** section:
   - ASCII diagram showing L0 → L1 flow
   - Example grep commands

Include HTML comments like `<!-- FILL: description -->` for guidance.
Keep under 100 lines.
  </action>
  <verify>grep -c "FILL:" .claude/skills/code-map/templates/MAP.md shows placeholder count</verify>
  <done>Complete template with all sections and FILL markers</done>
</task>

<task type="auto">
  <name>Task 2: Create L0 architecture template</name>
  <files>.claude/skills/code-map/templates/L0-architecture.md</files>
  <action>
Create architecture template with:

1. **Opening heading with anchor**: `# [L0:architecture] Project Name`

2. **System Overview** section:
   - One-paragraph description placeholder
   - Primary purpose and users

3. **Technology Stack** section:
   - Languages, frameworks, key dependencies
   - Table format: | Layer | Technology | Purpose |

4. **High-Level Architecture** section:
   - ASCII diagram placeholder showing major components
   - Data flow between components

5. **Domain Map** section:
   - List of L1 domains with standard markdown links
   - Example: `- [Conversion](domains/conversion.md) - Core conversion logic`
   - One-line description per domain

6. **Entry Points** section:
   - CLI commands, API endpoints, main functions
   - Use code links: `` [`main`](../src/cli.py#L15) ``

7. **Key Patterns** section:
   - Architectural patterns used
   - Where to find examples

Include size warning: "This file must stay under 500 lines."
All sections have FILL markers.
  </action>
  <verify>head -3 .claude/skills/code-map/templates/L0-architecture.md shows heading with anchor</verify>
  <done>Complete L0 template with all sections and anchor</done>
</task>

<task type="auto">
  <name>Task 3: Create L1 domain template</name>
  <files>.claude/skills/code-map/templates/L1-domain.md</files>
  <action>
Create domain template with:

1. **Opening heading with anchor**: `# [L1:domain-name] Domain Title`

2. **Purpose** section:
   - What this domain handles
   - Why it exists

3. **Entry Points** section:
   - Code links with line numbers
   - Example: `` [`convert_pack`](../src/converter.py#L42) - Main conversion entry ``
   - What each entry point does

4. **Depends On** section:
   - Standard markdown links to other domains
   - Example: `- [Database](database.md) - User storage`
   - External services/libraries

5. **Diagram** section:
   - ASCII diagram showing internal structure
   - Key components and their relationships

6. **Key Files** table:
   - | File | Purpose |
   - Critical files in this domain

7. **Deep Dive** section (optional):
   - Links to L2 modules: `[Component](modules/component.md)`
   - Only if L2 docs exist

Include size warning: "This file must stay under 300 lines."
All sections have FILL markers and [domain-name] placeholders.
  </action>
  <verify>head -3 .claude/skills/code-map/templates/L1-domain.md shows heading with anchor</verify>
  <done>Complete L1 template with all sections and anchor</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All three templates exist in templates/
- [ ] Each template has FILL markers for guidance
- [ ] Anchors follow correct syntax
- [ ] Size warnings included in L0 and L1 templates
</verification>

<success_criteria>

- All tasks completed
- Templates are copy-and-fill ready
- Format matches format-spec.md exactly
- Ready for Plan 03 (bootstrap workflow)
</success_criteria>

<output>
After completion, create `.planning/phases/06-code-map-skill/06-02-SUMMARY.md`
</output>
